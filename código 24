matriz = []
soma = 0

for i in range(3):
    linha = []
    for j in range(3):
        num = float(input(f"Digite [{i}][{j}]: "))
        linha.append(num)
        if i == j:
            soma += num
    matriz.append(linha)

print("Soma da diagonal principal:", soma)
