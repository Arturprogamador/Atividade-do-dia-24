matriz = []

for i in range(3):
    linha = []
    soma_linha = 0
    for j in range(3):
        num = float(input(f"Digite [{i}][{j}]: "))
        linha.append(num)
        soma_linha += num
    matriz.append(linha)
    print(f"Soma da linha {i}:", soma_linha)
